connectors:
  s3:
    type: s3
    chunk: !ENV ${CHUNK_SIZE}
    endpoint_url: https://s3.fr-par.scw.cloud
    region_name: fr-par
    bucket: !ENV ${STORAGE_BUCKET}
    aws_access_key_id: !ENV ${STORAGE_ACCESS_KEY}
    aws_secret_access_key: !ENV ${STORAGE_SECRET_KEY}
